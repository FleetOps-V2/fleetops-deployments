apiVersion: v2
name: platform
description: FleetOps Platform Infrastructure (Gateway + Proxy)
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: fleetops-common
    version: "1.0.0"
    repository: file://../common
