apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fleetops-request-prod
  namespace: argocd
  labels:
    app.kubernetes.io/name: fleetops-request-service
    app.kubernetes.io/environment: prod
    app.kubernetes.io/component: backend
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: default
  source:
    repoURL: https://github.com/FleetOps-V2/fleetops-deployments.git
    targetRevision: HEAD
    path: charts/request-service
    helm:
      valueFiles:
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: fleetops-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
